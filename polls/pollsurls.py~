from django.urls import path
from . import views
urlpatterns = [
    path('', views.index, name='index'),
    path('signup',templates/poll/signup.html, name="signup"),
    path('login',views.login, name="login")
]
